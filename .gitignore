# ESP-IDF build artifacts
build/
sdkconfig
sdkconfig.old

# Managed components are fetched on first build based on
# main/idf_component.yml + dependencies.lock. Don't commit them.
managed_components/

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db
