# Partition table for the Bala2 Fire (16 MB flash).
#
# Two ~3 MB OTA app slots so we can flash over Wi-Fi via the
# bala2_ota component. The bootloader picks the slot the otadata
# table points at; if the new image fails its first boot
# (no esp_ota_mark_app_valid_cancel_rollback() call), the bootloader
# rolls back to the previous slot. Anti-brick.
#
# Name,    Type, SubType,  Offset,    Size
nvs,       data, nvs,      0x9000,    0x6000
otadata,   data, ota,      0xf000,    0x2000
phy_init,  data, phy,      0x11000,   0x1000
ota_0,     app,  ota_0,    0x20000,   0x300000
ota_1,     app,  ota_1,    0x320000,  0x300000
storage,   data, fat,      0x620000,  0x9E0000
